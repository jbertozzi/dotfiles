[user]
name = Jeremy Bertozzi
email = jeremy.bertozi@gmail.com

[includeIf "gitdir:~/dev/github.com/"]
  path = ~/.gitconfig.github

[includeIf "gitdir:~/dev/github.foyer.lu/"]
  path = ~/.gitconfig.foyer

[alias]
  lg = log --graph -50 --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --tags=* origin/master --all

[help]
  autocorrect = 1 
[init]
	defaultBranch = master
[pull]
  rebase = true
[credential]
	helper = libsecret
